prmoji has been deployed.

Service:
  {{ include "prmoji.fullname" . }}:{{ .Values.service.port }}

If ingress is enabled, check your configured hosts under .Values.ingress.hosts.

This release expects an existing Secret named:
  {{ required "values.secret.existingSecret is required" .Values.secret.existingSecret }}
The Secret must contain keys: SLACK_TOKEN.
